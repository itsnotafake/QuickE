<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:textColor">@color/BLACK</item>
    </style>

    <!-- MainActivity styles -->
    <style name="StandardImageButton" parent="ThemeOverlay.AppCompat.Dark">
        <item name="colorButtonNormal">@color/colorPrimaryDark85Opac</item>
        <item name="colorControlHighlight">@color/colorPrimary</item>
    </style>

    <style name="StandardButton" parent="ThemeOverlay.AppCompat.Dark">
        <item name="colorControlHighlight">@color/colorPrimary</item>
    </style>

    <style name="sling_title_style">
        <item name="android:background">@color/WHITE45Opac</item>
        <item name="android:textAppearance">@style/Base.TextAppearance.AppCompat.Display1</item>
        <item name="android:textColor">@color/BLACK</item>
        <item name="android:textStyle">bold</item>
        <item name="android:maxLines">2</item>
        <item name="android:ellipsize">end</item>
    </style>

    <style name="sling_body_style">
        <item name="android:background">@color/WHITE45Opac</item>
        <item name="android:textAppearance">@style/Base.TextAppearance.AppCompat.Headline</item>
        <item name="android:textColor">@color/BLACK</item>
        <item name="android:maxLines">1</item>
        <item name="android:ellipsize">end</item>
    </style>

    <style name="browse_refresh_text">
        <item name="android:text">@string/browse_refresh_text</item>
        <item name="android:background">@color/TRANSPARENT</item>
        <item name="android:textAppearance">@style/Base.TextAppearance.AppCompat.Headline</item>
        <item name="android:textColor">@color/colorPrimary</item>
        <item name="android:gravity">center</item>
    </style>
</resources>
